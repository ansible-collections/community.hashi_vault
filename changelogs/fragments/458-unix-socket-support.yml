---
minor_changes:
  - community.hashi_vault collection - add support for connecting to a Vault server or ``vault-agent`` through a Unix socket by setting ``VAULT_ADDR`` (or the ``url`` option) to a ``unix://`` URL. Relies on the ``requests-unixsocket`` Python package (https://github.com/ansible-collections/community.hashi_vault/pull/458).
