---
- import_tasks: vault_server.yml
  # when: not vault_test_server_external | bool

- set_fact:
    vault_test_server_http: "{{ vault_test_server_http }}"
    vault_test_server_https: "{{ vault_test_server_https }}"
    vault_cert_file: "{{ vault_cert_file }}"
    vault_key_file: "{{ vault_key_file }}"
