---
- name: "Force output path to be in this role (which has existing files)"
  set_fact:
    output_dir: '{{ output_dir }}'

- import_role:
    name: setup_localenv_docker

- name: "Template the launch script"
  template:
    src: launch.sh.j2
    dest: '{{ output_dir }}/launch.sh'
    mode: '+x'
