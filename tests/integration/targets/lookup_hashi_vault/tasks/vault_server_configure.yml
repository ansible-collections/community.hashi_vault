---
- name: configure vault
  import_role:
    name: setup_vault_configure

- name: Configuration tasks
  vars:
    vault_addr: '{{ vault_test_server_http }}'
  block:
    #### auth method setup
    - name: setup auth methods
      import_tasks: vault_server_auth_setup.yml
